# CLAUDE.md

このファイルは、このリポジトリのコードを扱う際に Claude Code (claude.ai/code) および他のAIエージェントが参照するための単一の信頼できる情報源（Single Source of Truth）です。

> **ステータス:** 2026年サイト全面リニューアル進行中。現行の1ページLP（`index.html`）を廃止し、ハブ型サイトへ再構成する。事業背景の詳細は `IDENTITY.md` / `OFFERING.md` / `BRAND.md` / `WEBSITE.md` を参照（本ファイルと矛盾した場合は本ファイルを優先）。

---

## 1. プロジェクト概要

**Tech Connect** ― アプリ不要のWebARで「お客さんが足を止める体験」をつくるサービスのWebサイト。

- **最優先KPI:** 無料相談の問い合わせ件数を最大化する。すべての導線は相談フォームへ向ける。
- **今回のリニューアルで直す2つの課題:**
  - 課題A：1年前の受託制作（AR名刺・チラシ）向けの構成のままで、現在の事業（イベント向けARのレンタル、カスタマイズ済みデモを使った営業、自治体・イベント主催者へのアプローチ）とズレている。
  - 課題B：デザインに「AIっぽさ（汎用テンプレ感）」が残っている。→ 第5章のデザインルールで対策。

---

## 2. サイト構成（ハブ型）

現行の1ページLPを廃止し、トップ（ハブ）から各ページへ誘導する構成にする。

```
流入（検索・SNS・QR）
      ↓
トップページ（ハブ）… 実績と人柄で信頼をつくり、各所から相談へ誘導
      ├─ イベント・体験AR ページ（レンタル・出張・カスタマイズ／この分野の実績も掲載）
      ├─ 印刷物・商品AR ページ（名刺・チラシ・CD・パッケージ／この分野の実績も掲載）
      └─ AR入門コラム（旧「ARとは？」等の教育コンテンツの移転先）
      ↓
無料相談フォーム（全ページ共通の出口）
```

> **事例の扱い（決定）:** 当面は事例の独立ページ（`/works/`）を作らず、各サービスページ内の「実績」コーナーに統合する（実績が少ない段階で薄い独立ページを作ると信頼を下げ、ワンオペの運用負荷も上がるため）。1件で1ページ分の中身（写真・数字・お客様の声）を持てるほど育ったら、`/works/<案件名>/` として独立ページに昇格させる。

### URL設計・リダイレクト方針
- トップは `/`（現行と同じ）を維持する。
- 新ページ例：`/event-ar/`（イベント・体験AR。**2026-07 実装済み**、`event-ar/index.html`）、`/print-ar/`（印刷物・商品AR。**2026-07 実装済み**、`print-ar/index.html`）、`/column/<記事名>/`（コラム）。`/works/<案件名>/`（事例詳細）は将来、事例が育った段階で追加する。
- 下層ページ内のパスはルート絶対（`/style.css`、`/assets/...`）で書く（`/event-ar` と `/event-ar/` の揺れで相対パスが切れるのを防ぐため）。
- ページ分割で旧URLを変える場合は、`netlify.toml` に旧→新の 301（恒久的な転送）を追記する（**2026-07-12 `netlify.toml` 新設済み**。現在は `/index.html`→`/` 等のURL正規化のみ記載）。
- **【確認済み・低リスク】** 名刺・チラシに印刷済みのQRコード、および「AR体験する」ボタンの遷移先は、**このサイトとは別デプロイのAR体験URL**。よってサイトのページ分割でQRが切れる心配はない。ただし公開前チェックでは必ず実機で再確認する。

---

## 3. 技術スタックとホスティング

### ホスティング（実測に基づく事実）
- **本番ホスティング = Netlify**（独自ドメイン `techconnect-em.com`。DNSはGMO `gmoserver.jp` 管理）。
- **GitHubリポジトリ:** `github.com/techconnect-em/ar_lp`。
- ⚠️ 旧記述にあった「GitHub Pages（techconnect-em.github.io）メイン」「Cloudflare Pages」は実態と異なる。**正は Netlify 単独。**

### 主要ファイル構造
- `index.html`: 現行の1ページLP（リニューアルで分割予定）。
- `style.css`: Tailwind CSS（CDN）+ `:root` のCSS変数によるカスタムスタイル。
- `organic-particles-simple.js` / `ambient-particles.js`: Three.js 背景パーティクル（WebGLフォールバック付き）。
- `assets/`: 本番用画像・動画（1ファイル1MB以下を推奨。日本語ファイル名を含む）。

### スタック
- **Frontend**: Vanilla HTML/CSS/JavaScript（HTML/JSはビルド工程なし）
- **CSS**: Tailwind CSS (CDN) + カスタムユーティリティ
- **3D**: Three.js (r128)・モバイル最適化済み
- **Animations**: AOS (Animate On Scroll)
- **Forms**: Netlify Forms（`data-netlify="true"`、ハニーポット式スパム対策）。Netlifyホストなので現状そのまま動作する。リニューアル後も Netlify Forms を継承する。
- **アクセス計測 (GA4)**: **測定ID `G-PQ0NB9M678` を3ページに設定済み（2026-07-12）。** フォーム送信が成功した場合のみ `generate_lead` イベントを発火する（`/thanks` ページ方式は不採用＝送信成功判定をJSで持っているため）。公開後、GA4管理画面で `generate_lead` を「キーイベント」に指定すること。

### フォント（2026-07 実装の実態に合わせて更新）
- 見出し（`--font-display`）: `Zen Kaku Gothic New`
- 本文（`--font-body`）: `Noto Sans JP`
- 英字アイブロウ・数字（`--font-eyebrow`）: `Montserrat`
- ※ Google Fonts でロード。トップ・下層ともこの3書体で統一（Claude Design のデザインシステム準拠）。旧記述の「Outfit」は使用しない。

---

## 4. デザインルール（脱・AIっぽさ）

「AIっぽさ」＝均等なカードグリッドの羅列・絵文字の多用・汎用ワード・イラスト/アイコン中心の構成。以下をルール化する。

- **実写の写真・動画を主役**にする。イラスト・汎用アイコンは脇役に留める。
- **絵文字は一切使わない。**
- 同じ大きさのカードを等間隔に並べる構成を避け、サイズ・配置に**強弱**をつける。
- **色数を絞る**（ベース＋アクセント1色が基本）。
- 見出しに「〜とは？」「◯つのメリット」型を使わない。原稿で確定した見出し文言をそのまま使う。
- **CTAボタンは主・副の2種類のみ**。視覚的な強さに明確な差をつける。
- **モバイルファースト**（流入はQR・Instagram経由が中心でスマホ閲覧が大半）。

### デザイントークン（CSS `:root`、現行 `style.css` の実値＝正）
| 用途 | 変数 | 値 |
|------|------|----|
| メイン（紺・ダーク面） | `--navy-800` | `#142A52`（他に `--navy-900: #0E1E3D` / `--navy-700: #1B3360` / `--navy-600: #294a85`） |
| アクセント（黄・全体の1割まで） | `--yellow-500` | `#FCD400`（明 `--yellow-400: #FFDE2E` / 濃 `--yellow-600: #E8C000`） |
| ベース（薄灰） | `--light-gray` | `#F8F9FA` |
| 文字（濃灰） | `--dark-gray` | `#333333` |

> **配色は現行 `style.css` の実装値で確定（2026-07-12ユーザー決定）。** 旧記述の「紺 `#1c2f56`／黄 `#ffed00` へ統一」は取りやめ。Claude Designで承認済みの現行配色（紺 `#142A52` 系・黄 `#FCD400` 系）がブランド正式色となった。

> **下層ページのカテゴリー色（2026-07-05決定）:** トップの「テクコネクトができること」2カラムの色分けを下層ページ全体に継承する。イベント・体験AR＝紺（`--navy-800`）、**印刷物・商品AR＝グリーン**（`--green-600: #5E8C61`／ダーク面用 `--green-800: #2E4B33`）。/print-ar/ は `body.theme-green` でヒーロー・反転セクション・フッター・featuredプランをグリーンに上書きしている。黄色アクセントは全ページ共通。

---

## 5. コピー・メッセージング原則

- **キーワードは「体験」。** メインコピーは「お客さんが足を止める「体験」、つくります。」。「体験」は素のまま通すのが原則で、「感動」「驚き」は1〜2箇所だけ添える。
- NG言い換え：「サプライズ体験」「常識を覆す体験」（広告的な誇張＝AIっぽさの典型）。
- **ARという手段ではなく、お客さん側に起きる結果**（足を止める・記憶に残る・盛り上がる）を先に語る。
- 想定読者はAR知識ゼロのイベント主催者・店舗オーナー。**専門用語を避け、ですます調**で書く。
- 「ARとは？」「STEP解説」「メリット列挙」などの教育・型コンテンツはトップから外し、コラム・FAQへ移す。
- 相談ハードルを下げる言葉を各所に置く（「売り込みはしません」「思いつきの段階でOK」「断ってください」）。

---

## 6. サービス構成（顧客目的軸の2本柱＋相談枠）

技術ではなく「顧客が何をしたいか」で分類する。

- **柱1：イベント・体験AR**（マルシェ・商業施設・展示会・地域イベント向け）。提供モデルは**コンテンツの種類で2つ**（2026-07に「関わりの深さ3段階」を廃止。詳細は `イベントARページ引き継ぎ.md`）：
  - **ARゲーム（Catch The Fruits! / Finger Shot!）＝レンタルのみ**。借り方は「URLレンタル（客が規定スペックのPC・Webカメラ・モニターを用意）」と「出張レンタル（機材一式持込＋運営）」の2択。事前に動作確認用URLを渡す。**オリジナルARゲームの新規制作は提供しない**（相手側環境への依存が大きく動作保証しきれないため）。
  - **スマホ系（デジタルスタンプラリー・ARフォトフレーム）＝制作のみ**。来場者のスマホで完結（QR納品）。イベント専用に作るので使い回せない＝レンタルにしない。
  - **協賛（広告枠）**: ARゲームには協賛企業の画像を載せられる広告枠あり。枠自体は主催者に開放（協賛収入は主催者のもの）。Tech Connectがもらうのは掲載の作業費のみ＝広告画像の掲載作業 1枠¥3,000（5枠まで）／ネーミングライツ設定 ¥5,000。
- **柱2：印刷物・商品AR**（名刺・チラシ・カタログ・パッケージ・CD向け）。AR CD はここに含める（仕組み・顧客目的が名刺・チラシと同じため）。
- **相談枠（カスタム）:** 上記に当てはまらないもの（観光マップ・文化財演出・オリジナル企画）。独立した柱にはしない（実績の薄さを目立たせないため）。**実績が1件できたら3本目の柱に昇格**させる方針。

### イベント・体験ARの運用事実（2026-07確定。コピーはこの事実に合わせる）
- **ARゲーム（Catch The Fruits! / Finger Shot!）は来場者のスマホを使わない。** 会場に設置した画面（PC＋モニター＋Webカメラ）の前で体を動かして遊ぶ。1人ずつ交代制。スマホを使うのはスタンプラリー・ARフォトフレーム等（QR読み取り）。
- URLレンタル：規定スペックの機材（PC・モニター・Webカメラ）と会場のWi-Fiは主催者側が用意。規定スペックの数値はページに書かない（固まればFAQへ）。出張レンタルは機材・通信ともTech Connectが持込（会場の電源は借りる）。
- 納期：レンタルは最短3営業日でURL納品。カスタマイズは開催の2週間前までに申込み。制作は内容次第。
- **Finger Shot! は提供中（2026-07-12ユーザー確認）。** 旧「（近日提供開始）」表記はサイトから削除済み。
- **「人だかり」など集客を保証する表現はNG**（集客はイベント側の条件次第で保証できないため）。「足を止める」も下層ページでは使わない（トップ専用）。
- イベントページFVは「あれ、楽しかったね」と、後から語られるイベントに。（2026-07-02確定）

---

## 7. 料金（トップは目安のみ。詳細は各サービスページに一元化）

> **価格はすべて税別（2026-07-12確定）。** サイト上で「税込です」と総括する表記はNG。税込額を併記する場合は「（税込 ¥xx,xxx〜）」形式（/print-ar/ で採用中）。特商法表記は「サイトに表示された金額（税別）。消費税を別途申し受けます」。

- **印刷物・商品AR（名刺・チラシ等）:** キャンペーン ¥15,000〜（正規 ¥30,000〜）
- **印刷物・商品ARの体感AR（顔・手の動きに反応する上位プラン）:** ¥50,000〜（2026-07-11確定。実績あるARフォトフレーム等の仕組みを土台にする前提で、旧 ¥100,000〜 から変更）。**分類は認識技術でなく体験で切る:** マーカー・平面認識・空認識など「かざす・置くと現れる」系はすべて基本のAR ¥30,000〜の帯。平面・空は実機検証が済むまでサイトに明記しない（8th Wall機能の調査結果は `~/.claude/skills/marker-ar-8thwall/SKILL.md` の「対応機能メモ」参照）
- **ARゲーム レンタル:** URLレンタル 正規 ¥20,000〜（先着キャンペーン ¥15,000〜）【正規額は2026-07-02にユーザー確定。引き継ぎ書の¥25,000〜は上書き済み】／ 出張レンタル ¥50,000（7時間まで、超過 ¥7,500/時・遠方交通費別）／ カスタマイズ +¥30,000〜（レンタルに追加）
- **協賛・広告（作業費）:** 広告画像の掲載作業 1枠 ¥3,000（5枠まで・1社複数枠可）／ ネーミングライツ設定作業 ¥5,000
- **スマホ系 制作（スタンプラリー・ARフォトフレーム）:** ¥30,000〜（内容・規模で幅あり。キャンペーン額は条件未確定のため非表示）
- **廃止:** オリジナルARゲームの新規制作（旧 ¥300,000〜）。ゲームロジック変更 +¥100,000〜 の掲載も終了。
- **キャンペーン条件（レンタル・印刷物共通):** 先着10件・事例公開に協力いただける方限定
- **中心価格帯:** 出張運営やカスタマイズを含む依頼は ¥50,000〜¥150,000（本格的なオリジナル新規制作は ¥300,000〜）。

### 表示原則（厳守）
- 基準は**正規価格**。キャンペーン価格は割引として従属表示する（安価アンカーを防ぐため）。
- 中心価格帯（¥50,000〜150,000）のサイト掲載は **`/event-ar/` の料金注記のみ**。トップ・`/print-ar/` には載せない（2026-07-12ユーザー決定）。
- 具体的な金額は料金セクション／サービスページに**一元化**し、トップの他セクションに散らさない。
- **月額保守の3プラン（¥800 / ¥5,000 / ¥10,000）は廃止。** サポートは「動作保証＝納品後1ヶ月」「ホスティング継続＝**1年間は料金に含む。2年目以降の延長は有償**（条件は都度見積り、1ヶ月経過後は動作保証外）【2026-07-10確定】」「スポット対応＝都度見積り」に変更。「配信の仕組みを自社で運用」等、ホスティング基盤の自社運営と誤解される表現はサイトに書かない。
- WebARはスマホのOS・ブラウザに依存し将来の更新で表示が変わり得る旨を各サービスページに明記（「一度作れば永遠に動く」という誤解を防ぐ）。

---

## 8. 実績・社会的証明の素材（確定分）

- **音羽米稲刈り祭 歌舞伎ARクイズラリー**（豊川市・音羽エリア）：垂れ幕にスマホをかざすと地元歌舞伎の映像が出現。30秒視聴でクイズの答えがわかる仕掛け。**承諾済み（2026年6月、LINEで取得。表記は「JAひまわり 牧野様」）。** トップのお客様の声と /event-ar/ の実績に掲載中。
- **プロギング団体 TOTORUN（承諾済み）**：ARフォトフレーム付きスタンプカードの制作納品（豊川・豊橋エリア）。代表の声をトップに掲載中。
- **Catch The Fruits!**（マルシェでのARゲーム運用実績）
- **AR名刺**（既存実績）

> 表記マーカー：**【仮】**＝数値・文言が未確定（後で差し替え）。**【要承諾】**＝掲載許可待ち（承諾まで公開しない）。

---

## 9. 開発・制作ワークフロー

### ローカル開発
- サーバー起動：`python3 -m http.server 8000` → http://localhost:8000/
- CSS最適化が必要な場合のみ：`npx tailwindcss -i style.css -o style.bundle.css --watch`（開発中はCDN版を使用）

### 制作フロー（全体）
```
STEP1 素材の棚卸し（実写動画・写真・数字・声）
STEP2 コピー確定 … 完了（成果物：トップページ原稿）
STEP3 デザインシステム設定（色・フォント・コンポーネント）
STEP4 Claude Design でモックアップ制作（FVから1セクションずつ）
STEP5 handoff bundle を Claude Code に渡して実装 ←★ Claude Code の担当
STEP6 公開前チェック＋計測設定（GA4でフォーム送信をCV計測）
```

### コーディング規約
- **HTML/JS**: 4スペースインデント／**CSS**: 2スペースインデント
- 命名：顧客向けの言葉に沿った説明的なクラス名（例：`.hero-text-enhanced`）
- 文字コード：既存のUTF-8（日本語テキスト/ファイル名）は保持する

---

## 10. デプロイとGit運用

- **デプロイ:** Netlify（`techconnect-em.com`）。GitHub `main` への push で連動。
- **Git:** 変更は基本 `git push origin main`。コミットメッセージは命令形・英語・センテンスケース（例：`Restructure top page into hub layout`）。`.DS_Store` はコミットしない。

---

## 11. 更新ガイドライン・公開前チェック

- **ナビゲーション:** デスクトップ・モバイル・フッターの3箇所を同期して更新する。
- **スタイル変更:** パーティクルへの影響を考慮し、必ずモバイル実機またはエミュレーターで確認する。Lighthouseスコアの大幅低下（5点以上）に注意。
- **SEO基盤（2026-07-12 GPTクロスレビューを経て整備済み）:** canonical・sitemap.xml・robots.txt・404.html・netlify.toml・JSON-LD（トップ=Organization+WebSite、下層=Service+BreadcrumbList）・下層パンくず・下層titleの検索語前方化（h1と`og:title`は現コピー維持）・対応エリアFAQ（下層2ページ）・`og:locale`追加・`twitter:site`削除（非所有アカウントだったため）。**公開後の残り: Google Search Console登録＋sitemap送信、Googleビジネスプロフィール整備、ページ別OGP画像。**
- **公開前チェック:**
  - iOS Safari / Android Chrome 実機確認
  - 動画の軽量化（`assets/制作実績/` は1本1〜2MB目安。**2026-07-12 ページ参照中の全9本を再エンコード済み＝最大2.1MB**）、OGP設定、GA4のCV計測設定
  - **2026-07-12 ページ未参照の重い元素材（約70MB分・32ファイル）は `~/Documents/TechConnect_ar_lp_退避素材/` へ退避済み。** `assets/` は83MB→23MBに縮小。今後もページに使わなくなった元素材はこの退避フォルダへ移す運用とする
  - フォーム送信テストは**トップと `/event-ar/` の両方から**実施し、Netlify管理画面で「流入ページ」フィールド（hiddenで送信元ページ名を送る仕組み）が届いているか確認する
  - 旧コンテンツの引っ越し（「ARとは？」等→コラム、料金詳細→各サービスページ）
  - プライバシーポリシー／特定商取引法に基づく表記の継承（現行は `index.html` 内モーダル。分割後は適切なページへ）
  - 既存QRコードの遷移先が生きているか実機で再確認（別デプロイのため低リスクだが必須）

---

## 12. 作業の進め方・品質基準（AIエージェント向け）

### 12.1 品質バーとレビュー
ページを公開する前に、次の3層のレビューを通すこと。
1. **セルフレビュー:** 実装直後に自分で見直す（壊れ・重複・置き去りのプレースホルダ）。
2. **自動レビュー:** `/code-review`（バグ・簡素化）と `/security-review`（セキュリティ）を実行する。
3. **2視点クロスレビュー:** 下記サブエージェント2体に並行レビューを依頼し、指摘を統合して反映する。

### 12.2 セキュリティ（このプロジェクトの範囲）
静的サイト＋Netlifyフォームのため攻撃面は小さいが、以下は必ず守る。
- 秘密情報（APIキー・トークン等）をリポジトリにコミットしない。`.env` 等は `.gitignore`。
- 外部リンクは `rel="noopener noreferrer"`。
- フォームはNetlifyのハニーポット対策を維持。個人情報の扱いはプライバシーポリシーと整合させる。
- 第三者のCDN/スクリプトは信頼できるものに限定。可能なら SRI（`integrity`）を付ける。
- フォーム等のJSでユーザー入力を `innerHTML` 等で危険に展開しない。
- 依存（Three.js 等）はバージョンを固定する。

### 12.3 トークン効率・実装計画（本当の節約はここ）
- **1ページ／1セクション単位**で進める。一度に全部作らない。
- 大きなファイルは**必要な箇所だけ読む**（全文を何度も読み直さない）。
- **デザインシステム（色・余白・コンポーネント）と共通部品（ヘッダー/フッター/フォーム）を使い回す**。毎回ゼロから作らない。
- 既存実装（フォーム・パーティクル）は流用する。
- サブエージェントは**合計トークンを減らす道具ではない**。品質と並行作業のための道具であり、要所でのみ使う（小さな修正・文言変更には使わない）。

### 12.4 サブエージェント運用（2体のレビュー体制）
`.claude/agents/` に2体を用意済み。いずれも**読み取り専用（自分では変更しない）**で、指摘・改善案を返す。適用はメインの担当が行う。
- **シニアエンジニア（`senior-engineer`）:** 正確性・パフォーマンス・モバイル堅牢性・アクセシビリティ・セキュリティ・保守性。
- **プロデザイナー（`pro-designer`）:** 視覚的階層・脱AIっぽさ・ブランド整合（色/フォント/余白）・余白とリズム・実写主役。

**使いどころ:**
- ① ページが一通り完成したら、2体を**並行で**走らせてクロスレビュー → 指摘を統合して反映。
- ② 独立した複数セクション/ページがある場合の分担制作（同じファイルを同時に編集させないこと）。

**使わない場面:** 1〜数行の小修正、文言だけの変更。

> 注：2体は現在「レビュー専門（読み取り専用）」。エージェント自身にコードを書かせたい場合は、各定義ファイルの `tools` に `Edit, Write` を追加する。
